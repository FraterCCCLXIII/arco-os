/** IPC channel names shared between main and preload. */
const DESKTOP_IPC = {
  minimizeWindow: "lf:minimize-window",
  maximizeWindow: "lf:maximize-window",
  closeWindow: "lf:close-window",
};

module.exports = { DESKTOP_IPC };
