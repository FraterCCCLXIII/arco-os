/** Tailwind class map — converted from SidebarPane.module.css */
const styles: Record<string, string> = {
  pane: "h-full min-w-0 overflow-hidden",
  resizable: "[flex-shrink:0]",
};
export default styles;
