/** Tailwind class map — converted from TrayScaleFit.module.css */
const styles: Record<string, string> = {
  content: "w-[max-content]",
  sizer: "[flex-shrink:0]",
  viewport: "flex justify-center items-end w-full max-w-full min-w-0 overflow-hidden",
};
export default styles;
