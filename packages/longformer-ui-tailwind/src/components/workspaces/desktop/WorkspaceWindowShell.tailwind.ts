/** Tailwind class map — converted from WorkspaceWindowShell.module.css */
const styles: Record<string, string> = {
  shell: "flex-1 h-full min-h-0 min-w-0 flex flex-col overflow-hidden",
};
export default styles;
