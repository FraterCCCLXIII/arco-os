/** Tailwind class map — converted from ContactsWorkspace.module.css */
const styles: Record<string, string> = {
  detailPane: "flex-1 min-w-0 min-h-0 hidden hidden",
  dialHeader: "[flex-shrink:0] p-[var(--lf-space-3)_var(--lf-space-4)] text-lf-sm font-semibold text-lf-text-tertiary uppercase [letter-spacing:0.04em] border-b border-lf-divider",
  dialPane: "h-full flex flex-col min-h-0 bg-lf-surface-sunken",
  dialResizable: "hidden hidden",
  listResizable: "w-full w-full",
  listScroll: "flex-1",
  workspace: "h-full flex min-h-0",
};
export default styles;
