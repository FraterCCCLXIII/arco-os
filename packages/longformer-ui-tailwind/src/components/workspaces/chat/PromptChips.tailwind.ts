/** Tailwind class map — converted from PromptChips.module.css */
const styles: Record<string, string> = {
  row: "flex flex-nowrap gap-lf-2 w-[max-content]",
  wrapper: "min-w-0 w-full [overflow-x:auto] overflow-y-hidden",
};
export default styles;
