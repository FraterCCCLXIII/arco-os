/** Tailwind class map — converted from ChatContextPanel.module.css */
const styles: Record<string, string> = {
  collapsedRail: "[flex-shrink:0] flex flex-col items-center gap-lf-1 p-[var(--lf-space-2)_var(--lf-space-1)] border-l border-lf-divider bg-lf-surface-2",
  conversation: "flex-1 min-w-[240px] [border-right:none]",
  drawerFull: "flex-1 min-w-0 h-full",
  drawerPane: "h-full min-h-0",
  drawerResizable: "[flex-shrink:0] overflow-visible",
  drawerResizeHandle: "z-[3]",
  panel: "h-full flex min-h-0 min-w-0 overflow-visible",
  panelDrawerOnly: "w-full",
};
export default styles;
