/** Tailwind class map — converted from TasksWorkspace.module.css */
const styles: Record<string, string> = {
  calendarPane: "h-full flex flex-col bg-lf-surface-sunken",
  calendarScroll: "h-full p-[var(--lf-space-4)_var(--lf-space-3)]",
  group: "mb-lf-5",
  groupHeader: "flex items-center gap-lf-2 mb-lf-2 text-lf-sm font-semibold text-lf-text-tertiary uppercase [letter-spacing:0.03em]",
  header: "[flex-shrink:0] flex items-center justify-between h-[44px] p-[0_var(--lf-space-4)] border-b border-lf-divider",
  headerTitle: "flex items-center gap-lf-2 font-semibold text-lf-text-primary",
  main: "flex-1 min-w-0 flex flex-col min-h-0",
  page: "max-w-[720px] m-[0_auto] p-[var(--lf-space-5)_var(--lf-space-5)_var(--lf-space-8)]",
  scroll: "flex-1",
  workspace: "h-full flex flex-row min-h-0",
};
export default styles;
