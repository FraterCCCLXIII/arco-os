/** Tailwind class map — converted from AppGrid.module.css */
const styles: Record<string, string> = {
  grid: "grid grid-cols-[repeat(auto-fill,_minmax(88px,_1fr))] gap-lf-5 p-lf-1",
};
export default styles;
